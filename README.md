# Scrum Platform

Monorepo implementation of a Scrum sprint management application.

## Stack
- Backend: Node.js + TypeScript + NestJS + Prisma + PostgreSQL
- Frontend: React + TypeScript + MobX + controller-oriented architecture
- Contracts: Shared TypeScript DTO package
- Runtime: Docker Compose for `postgres`, `api`, `web`

## Implemented Features
- Local auth (`signup`, `login`, `refresh`, `logout`) and profile update endpoints.
- GitLab OAuth start + callback with account linking by GitLab ID/email.
- Role-based guards with agile roles (`platform_admin`, `product_owner`, `scrum_master`, `team_member`).
- Teams CRUD + member assignment endpoints.
- Products CRUD + member assignment + configurable workflow columns.
- Product backlog stories CRUD + ranking endpoint.
- Tasks CRUD + status changes + assignee/sprint assignment.
- User-managed API keys from Settings and MCP task operations authenticated by `x-api-key`.
- Sprints CRUD + start/complete actions + sprint board endpoint.
- Indicators endpoints for burnup/burndown and velocity by team/user.
- React routes/views for login/signup/profile/admin/teams/products/backlog/tasks/sprint management/sprint execution/indicators.
- Admin role management view (`/admin`) to change user roles.

## Repository Layout
- `apps/api`: NestJS API + Prisma schema
- `apps/web`: React web client
- `packages/contracts`: Shared DTO and type contracts
- `packages/ui`: Shared UI package placeholder

## Local Setup
1. Copy `.env.example` to `.env` and adjust values.
2. Start Postgres: `docker compose up -d postgres`
3. Install dependencies: `pnpm install`
4. Prepare database and Prisma client: `pnpm db:push && pnpm db:generate && pnpm db:seed`
5. Start apps in development: `pnpm dev`
   This command now runs the database sync and default seed before starting the apps, and stops early if the schema/client preparation fails. The dev frontend is exposed by nginx on `https://vmi3181573.contaboserver.net:5443` and the dev API on `https://vmi3181573.contaboserver.net:5444`.
6. Publish production build: `pnpm build`
   This writes fresh artifacts into `deploy/web` and `deploy/api`, restarts the production API and MCP on internal ports `3100/3101`, and nginx serves the updated production site on `https://vmi3181573.contaboserver.net`.

## Default Seed Users
- `admin@scrum.local` / `admin1234` (`platform_admin`)
- `owner@scrum.local` / `owner1234` (`product_owner`)
- `scrum@scrum.local` / `scrum1234` (`scrum_master`)
- `member@scrum.local` / `member1234` (`team_member`)

## Validation
- Typecheck: `pnpm typecheck`
- Build: `pnpm build`

## User Guide Notes
- User activity stats shown in `/admin` use a time window (`week`, `month`, `semester`, `year`) and mean:
- `Eventos`: total activity log records generated by the selected user in the chosen window.
- `Dias activos`: number of distinct calendar days in which the user generated at least one activity log.
- `Velocidad promedio`: average story points completed by that user per completed sprint in the chosen window. It is `0` when there are no completed sprints with finished work assigned to that user.
- `Tareas trabajadas`: number of distinct tasks the user touched in the chosen window, not the number of task events.
- `Acciones de sprint`: number of sprint-scoped activity records generated by that user in the chosen window.
- Sprint completion follows Scrum lock rules in this app: when a sprint is completed, unfinished tasks are removed from that sprint and returned to the task pool so the scrum master can decide whether to re-plan them into a future sprint.
- Task, sprint, product and team descriptions accept markdown and are rendered as markdown in workspace and admin views.
- Production nginx owns ports `443`, `3000`, and `3001`.
- Development nginx proxies `5443` to the Vite dev server on internal port `5000` and `5444` to the API dev server on internal port `5001`.
- MCP HTTP server runs on `MCP_PORT` (default `3101`) and also accepts `/mcp` on the main API app. Supported tools: list focused pending tasks, take unassigned tasks, change status of self-assigned tasks, and comment on self-assigned tasks.
- Uploaded media is shared between prod and dev through `/root/repos/scrum/shared/media`.

## Current Gaps
- Indicator calculations are functional but simplified for v1.
- E2E/integration tests and observability hardening are not yet implemented.
